.. include:: ../_shared_nav.rst
Direct Installation
===================

1. Install Dexsuite:

   .. code-block:: bash

      pip install dexsuite

2. Install required dependencies (from the repo):

   .. code-block:: bash

      cd Dexsuite
      pip install -r requirements.txt
